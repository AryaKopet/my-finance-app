const Header = () => {
  return (
    <header className="w-full h-16 bg-white shadow-sm px-6 flex items-center justify-between fixed left-64 top-0 z-10">
      <h2 className="text-xl font-semibold text-gray-800">Dashboard</h2>
      <div className="text-sm text-gray-500">Halo, kawan 👋</div>
    </header>
  );
};

export default Header;
